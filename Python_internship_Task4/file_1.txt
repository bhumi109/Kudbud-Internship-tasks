#Automate File Renaming in a Folder
import os

def rename_files(folder_path):
    # Get all files from the folder
    files = os.listdir(folder_path)
    
    # Sort files so naming is consistent
    files.sort()

    count = 1
    for filename in files:
        # Create full path for the old file name
        old_path = os.path.join(folder_path, filename)
        
        # Skip if it's a directory
        if os.path.isdir(old_path):
            continue
        
        # Create new file name
        new_filename = f"file_{count}.txt"
        new_path = os.path.join(folder_path, new_filename)
        
        # Rename operation
        os.rename(old_path, new_path)
        print(f"Renamed: {filename}  →  {new_filename}")

        count += 1


# 📌 Enter your folder path here
folder_path = r"C:\Users\HP\Desktop\Python_Developer_Internship"
rename_files(folder_path)
